<h4 align="center">Welcom to <%= @user.username %>'s page</h4>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user%>
  </div>
</div>
<h4 align="center"><%= @user.username %>'s Articles</h4>
<%= render 'articles/articles', obj: @user.articles %>
